import { EchartsDataZoomDirective } from './echarts-data-zoom.directive';

describe('EchartsDataZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new EchartsDataZoomDirective();
    expect(directive).toBeTruthy();
  });
});
